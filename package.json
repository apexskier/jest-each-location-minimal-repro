{
  "name": "test",
  "version": "1.0.0",
  "devDependencies": {
    "jest": "^26.4.0"
  },
  "scripts": {
    "test": "jest",
    "example": "CI=true jest --testLocationInResults --no-watchman --reporters ./reporter.js "
  }
}
